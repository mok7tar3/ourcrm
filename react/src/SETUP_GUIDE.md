# KSU Ticketing System - دليل الإعداد للمبرمجين

## 📋 نظرة عامة
هذا المشروع تم تصديره من Figma Make وهو جاهز للتطوير باستخدام React + TypeScript + Vite + Tailwind CSS.

---

## 🚀 التثبيت والتشغيل السريع

### المتطلبات الأساسية
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn أو pnpm

### خطوات التثبيت

1. **تثبيت المكتبات:**
```bash
npm install
```

2. **تشغيل المشروع في وضع التطوير:**
```bash
npm run dev
```

3. **بناء المشروع للإنتاج:**
```bash
npm run build
```

4. **معاينة النسخة المبنية:**
```bash
npm run preview
```

---

## ⚠️ تعديلات مطلوبة بعد التصدير

### 1️⃣ الصور المستوردة من Figma

الملفات التالية تحتوي على استيرادات صور بصيغة `figma:asset` يجب تحويلها:

#### ملف: `/src/components/Navbar.tsx`
```typescript
// قبل التعديل:
import imgLogo from "figma:asset/99dd989727a003d1451e90bb5ff2c09e4c24de63.png";

// بعد التعديل:
import imgLogo from "@/assets/images/logo.png";
```

#### ملف: `/src/imports/HeroSection.tsx`
```typescript
// قبل التعديل:
import imgHeroImage1 from "figma:asset/854fd69d0b03c80d9e26e8ef207b58548b2f4940.png";

// بعد التعديل:
import imgHeroImage1 from "@/assets/images/hero-image.png";
```

#### ملف: `/src/imports/AboutOurKsu.tsx`
```typescript
// قبل التعديل:
import imgAboutHeroImage1 from "figma:asset/98ba85f888287ff174bf52e04d60cd8363aeaff3.png";

// بعد التعديل:
import imgAboutHeroImage1 from "@/assets/images/about-image.png";
```

#### ملف: `/src/imports/OurNumbersSection.tsx`
```typescript
// قبل التعديل:
import imgOurNumbersSection from "figma:asset/2de6f0096401685cbf6679b7498e485ca7a407bb.png";

// بعد التعديل:
import imgOurNumbersSection from "@/assets/images/stats-image.png";
```

### 2️⃣ إنشاء مجلد الصور

قم بإنشاء المجلد التالي ونقل الصور إليه:
```
/src/assets/images/
  ├── logo.png
  ├── hero-image.png
  ├── about-image.png
  └── stats-image.png
```

**📌 ملاحظة:** يمكنك تصدير الصور من Figma مباشرة باستخدام:
1. افتح ملف Figma
2. اختر الصورة المطلوبة
3. انقر بزر الماوس الأيمن → Export → PNG (2x أو 3x للجودة العالية)

---

## 📁 هيكل المشروع

```
ksu-ticketing-system/
├── public/                  # الملفات الثابتة العامة
├── src/
│   ├── assets/             # الصور والملفات
│   │   └── images/        # صور المشروع
│   ├── components/         # المكونات القابلة لإعادة الاستخدام
│   │   ├── Layout.tsx     # هيكل الصفحة (Navbar + Footer)
│   │   └── Navbar.tsx     # شريط التنقل
│   ├── contexts/          # React Contexts
│   │   └── LanguageContext.tsx  # إدارة اللغة (عربي/إنجليزي)
│   ├── imports/           # المكونات المستوردة من Figma
│   │   ├── HeroSection.tsx
│   │   ├── AboutOurKsu.tsx
│   │   ├── HowItWorksSecton.tsx
│   │   ├── OurNumbersSection.tsx
│   │   ├── FaqSection.tsx
│   │   ├── CatSection.tsx
│   │   ├── Footer.tsx
│   │   └── svg-*.ts       # ملفات SVG
│   ├── pages/             # صفحات التطبيق
│   │   ├── HomePage.tsx
│   │   └── UnderDevelopment.tsx
│   ├── styles/            # ملفات التنسيق
│   │   └── globals.css    # Tailwind + Custom styles
│   ├── App.tsx            # المكون الرئيسي + Routing
│   └── main.tsx           # نقطة الدخول
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 🌐 المسارات المتاحة (Routes)

| المسار | الصفحة | الحالة |
|--------|--------|--------|
| `/` | الصفحة الرئيسية | ✅ جاهز |
| `/search` | البحث عن تذكرة | 🚧 تحت التطوير |
| `/faq` | الأسئلة الشائعة | 🚧 تحت التطوير |
| `/about` | من نحن | 🚧 تحت التطوير |
| `/knowledge` | قاعدة المعرفة | 🚧 تحت التطوير |

---

## 🎨 المكتبات المستخدمة

### الأساسية
- **React 18.2** - مكتبة بناء واجهة المستخدم
- **TypeScript** - لغة البرمجة المكتوبة
- **Vite** - أداة البناء السريعة
- **React Router DOM 6** - التنقل بين الصفحات

### التنسيق
- **Tailwind CSS 4.0** - إطار عمل CSS
- **Lucide React** - مكتبة الأيقونات

---

## 🌍 نظام اللغات (Bilingual Support)

المشروع يدعم اللغتين العربية والإنجليزية بالكامل مع RTL.

### كيفية إضافة ترجمات جديدة:

في ملف `/src/contexts/LanguageContext.tsx`:

```typescript
const translations = {
  en: {
    nav: {
      home: 'Home',
      newKey: 'New Text',  // أضف هنا
    }
  },
  ar: {
    nav: {
      home: 'الرئيسية',
      newKey: 'نص جديد',  // أضف هنا
    }
  }
};
```

### الاستخدام في المكونات:
```typescript
import { useLanguage } from '@/contexts/LanguageContext';

function MyComponent() {
  const { t, language, toggleLanguage } = useLanguage();
  
  return <p>{t('nav.newKey')}</p>;
}
```

---

## 🎯 الميزات المطبقة

✅ **تصميم متجاوب كامل** - يعمل على جميع الأجهزة
✅ **دعم اللغتين عربي/إنجليزي** - مع RTL كامل
✅ **تأثيرات Fade-in احترافية** - باستخدام Intersection Observer
✅ **تأثير العد التصاعدي** - على الأرقام في قسم الإحصائيات
✅ **تأثيرات الكتابة** - Typewriter effect في العنوان الرئيسي
✅ **نظام توجيه** - React Router للتنقل بين الصفحات
✅ **Navbar ثابت** - موجود في جميع الصفحات
✅ **Footer ثابت** - موجود في جميع الصفحات

---

## 🔧 التطوير المستقبلي

### صفحات تحتاج للتنفيذ:
1. **صفحة البحث** (`/search`) - البحث عن تذاكر موجودة
2. **صفحة الأسئلة الشائعة** (`/faq`) - عرض الأسئلة الشائعة
3. **صفحة من نحن** (`/about`) - معلومات عن الجامعة
4. **صفحة قاعدة المعرفة** (`/knowledge`) - مقالات المساعدة

### وظائف مقترحة:
- 🔐 نظام تسجيل الدخول
- 📝 إنشاء تذاكر جديدة
- 📊 لوحة تحكم المستخدم
- 🔔 نظام الإشعارات
- 📱 تطبيق موبايل (React Native)
- 🌙 الوضع الليلي (Dark Mode)

---

## 📝 ملاحظات مهمة

### Tailwind CSS 4.0
المشروع يستخدم Tailwind CSS 4.0 بدون ملف `tailwind.config.js` - جميع التخصيصات في `/src/styles/globals.css`.

### الخطوط (Fonts)
الخطوط المستخدمة:
- `Inter` - للنصوص الإنجليزية
- `IBM Plex Sans Arabic` - للنصوص العربية

تأكد من تحميل هذه الخطوط في `globals.css` أو استخدام Google Fonts.

### المكونات المحمية
**لا تقم بتعديل هذا الملف:**
- `/src/components/figma/ImageWithFallback.tsx` (ملف محمي من النظام)

---

## 🐛 استكشاف الأخطاء

### مشكلة: الصور لا تظهر
**الحل:** تأكد من تحويل جميع استيرادات `figma:asset` إلى مسارات محلية كما موضح في القسم 1️⃣ أعلاه.

### مشكلة: TypeScript Errors
**الحل:** قم بتشغيل:
```bash
npm install --save-dev @types/react @types/react-dom
```

### مشكلة: Tailwind styles لا تعمل
**الحل:** تأكد من أن `globals.css` مستورد في `main.tsx`:
```typescript
import './styles/globals.css'
```

---

## 📞 الدعم

للأسئلة أو المشاكل، يرجى التواصل مع فريق التطوير.

---

## 📄 الترخيص

هذا المشروع ملك لجامعة الملك سعود.

---

**تم التصدير من Figma Make | آخر تحديث: ديسمبر 2025**
